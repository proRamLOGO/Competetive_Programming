#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <string.h>

using namespace std ;

long long int main() {

    long long int t ;
    cin >> t ;

    while ( t-- ) {

        long long int n, k, m ;
        cin >> n ;
        vector< long long int > v(n) ;

        for ( auto i = 0 ; i < n ; ++i )
            cin >> v[i] ;




    }

}